import {StyleSheet} from 'react-native';

const text = StyleSheet.create({
  white: {
    color: '#ffffff'
  },
  black: {
    color: '#000000'
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold'
  }
});

export default text;
